## Customize the test machine
machine:
  timezone:
    America/New_York # Set the timezone
  java:
    version: oraclejdk8

## Customize checkout
checkout:
  post:
    - git submodule sync
    - git submodule update --init # use submodules